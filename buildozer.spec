# (list) Application requirements
# comma separated e.g. requirements = sqlite3,kivy
requirements = python3,kivy,kivymd,opencv-python,pyttsx3,torch,numpy,ultralytics

# (str) Package name
package.name = videocaptureapp

# (str) Package domain (needed for android/ios packaging)
package.domain = org.example

# (str) Source code where the main.py live
source.include_exts = py,png,jpg,kv,atlas

# (list) Application source files
source.include_exts = py,png,jpg,kv,atlas
source.include_patterns = assets/*,images/*,fonts/*

# (list) Application source files (python files to include)
source.include_exts = py,png,jpg,kv,atlas

# (list) Application source files (python files to include)
source.include_exts = py,png,jpg,kv,atlas
source.include_patterns = assets/*,images/*,fonts/*

# (str) Custom source folders for requirements
source.include_exts = py,png,jpg,kv,atlas
source.include_patterns = assets/*,images/*,fonts/*
source.include_exts = py,png,jpg,kv,atlas
source.include_patterns = assets/*,images/*,fonts/*

# (str) Package name
package.name = videocaptureapp

# (str) Package domain (needed for android/ios packaging)
package.domain = org.example

# (str) Application versioning (method 1)
version = 0.1

# (list) Application requirements
# comma separated e.g. requirements = sqlite3,kivy
requirements = python3,kivy,kivymd,opencv-python,pyttsx3,torch,numpy,ultralytics

# (str) Custom source folders for requirements
source.include_exts = py,png,jpg,kv,atlas
source.include_patterns = assets/*,images/*,fonts/*

# (str) Application versioning (method 1)
version = 0.1

# (str) Application versioning (method 2)
version.regex = __version__ = ['"](.*)['"]
version.filename = %(source.dir)s/main.py

# (list) Application requirements
# comma separated e.g. requirements = sqlite3,kivy
requirements = python3,kivy,kivymd,opencv-python,pyttsx3,torch,numpy,ultralytics

# (str) Custom source folders for requirements
source.include_exts = py,png,jpg,kv,atlas
source.include_patterns = assets/*,images/*,fonts/*

# (str) Application versioning (method 2)
version.regex = __version__ = ['"](.*)['"]
version.filename = %(source.dir)s/main.py

# (list) Garden requirements
#garden_requirements = sqlite3,kivy
garden_requirements = kivy,kivymd

# (str) Custom source folders for requirements
source.include_exts = py,png,jpg,kv,atlas
source.include_patterns = assets/*,images/*,fonts/*

# (str) Application versioning (method 2)
version.regex = __version__ = ['"](.*)['"]
version.filename = %(source.dir)s/main.py

# (list) Garden requirements
#garden_requirements = sqlite3,kivy
garden_requirements = kivy,kivymd

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (int) Application version code
#source.include_exts = py,png,jpg,kv,atlas
#source.include_patterns = assets/*,images/*,fonts/*

# (str) Application versioning (method 2)
version.regex = __version__ = ['"](.*)['"]
version.filename = %(source.dir)s/main.py

# (list) Garden requirements
#garden_requirements = sqlite3,kivy
garden_requirements = kivy,kivymd

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (int) Application version code
version.code = 1

# (int) Application version code
version.code = 1

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (int) Application version code
version.code = 1

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (int) Application version code
version.code = 1

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (int) Application version code
version.code = 1

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (int) Application version code
version.code = 1

# (str) Android/IOS display name
package.name = VideoCaptureApp

# (str) Android/IOS package name
package.name = videocaptureapp

# (int) Application version code
version.code = 1

# (list) Application requirements
# comma separated e.g. requirements = sqlite3,kivy
requirements = python3,kivy,kivymd,opencv-python,pyttsx3,torch,numpy,ultralytics

# (str) Custom source folders for requirements
source.include_ext
